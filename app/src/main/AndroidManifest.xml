<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 监听网络恢复后自动重连(registerNetworkCallback 需要 ACCESS_NETWORK_STATE) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 版本自动更新:下载新 APK 后拉起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 扫码绑定:用后置摄像头扫描远程二维码 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ 下载/更新进度通知需要运行时申请 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 扫码识别成功时短震动反馈(普通权限,安装即授予,无需运行时申请) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 相机非硬性要求,无摄像头设备仍可安装,只是扫码不可用 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- allowBackup=false:SharedPreferences 里的远程链接带 sid(等于控制电脑的凭证),禁止云备份/adb backup 外泄;
         data_extraction_rules 进一步禁掉 Android 12+ 的云备份与设备迁移 -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="ZCode Remote"
        android:theme="@style/Theme.App">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="zcode.z.ai"
                    android:pathPrefix="/remote" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>
</manifest>
