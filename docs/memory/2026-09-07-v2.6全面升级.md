# 2026-09-07 v2.6 全面升级(可靠性补课 + 功能增量 + 拆分)

- **范围**:按 2026-09-07 升级分析的第一/二档全做——①`onRenderProcessGone` 渲染崩溃自愈(销毁死 WebView 按当前链接重建会话层);②预测性返回(Manifest `enableOnBackInvokedCallback` + API 33+ 注册 `OnBackInvokedCallback`,与 `onBackPressed` 共用 `handleBack`);③更新链路拆为 `Updater.java`(MainActivity 2057→约 1700 行);④菜单"出示会话码"(zxing QRCodeWriter 编码当前链接,白底衬垫,60s 倒计时后连衬垫一起遮蔽);⑤长按图标动态快捷方式=最近两条历史会话(`ACTION_OPEN_SESSION` + route 处理);⑥应用锁(框架 BiometricPrompt,默认关,进程内验一次,未录生物识别走降级弹窗)。targetSdk 35 与前台服务保活按分析结论**不做**。
- **验证**(模拟器 zc,全过):菜单/QR 对话框/遮蔽态截图亲眼确认;QR 截图用宿主机 zxing core 解码回原文逐字符一致;`kill -9` webview sandboxed 进程后 App 存活且会话层重建;返回键三层语义(覆盖层回会话/双击退出提示/finish)在 API 34 预测性返回路径下全通;`dumpsys shortcut` 见 session_0/1;应用锁开关持久化+冷启动降级弹窗;手动检查更新链路跑通(双 Toast 被 SystemUI 竞态吞第二条,见踩坑)。
- **版本号**:versionCode 16→17、versionName 2.5→2.6;界面版本运行时读 PackageInfo 无需第三处同步。**未发版**(用户未说"发布"),APK 仅在本地 build 产物。
- **新增权限**:仅 `USE_BIOMETRIC`(普通权限,应用锁需要),符合"新增权限先确认必要"边界。
- **沉淀**:耐久坑进 AGENTS.md §踩坑记录(预测性返回不回调 onBackPressed/杀 sandboxed 验自愈/SystemUI 双 Toast 竞态/动态快捷方式启动同步/模拟器无生物识别);§关键路径加 Updater.java;§原则 2、3 文案按拆分与运行时取版本更新。
